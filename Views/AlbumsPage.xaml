<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Photon.Views.AlbumsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Loaded="OnPageLoaded">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0" DefaultLabelPosition="Right" HorizontalContentAlignment="Stretch" Margin="8,8,8,0">
            <CommandBar.Content>
                <TextBlock Text="Albums" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center" Margin="4,0,0,0"/>
            </CommandBar.Content>
            <AppBarButton Icon="Add" Label="New album" Click="OnCreateAlbum"/>
        </CommandBar>

        <GridView x:Name="AlbumsGrid" Grid.Row="1" Padding="32,16,32,32" SelectionMode="None">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Grid Width="300" Height="260" CornerRadius="8" 
                          Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" 
                          RightTapped="OnAlbumRightTapped" Tapped="OnAlbumTapped">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="160"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Cover Image -->
                        <Image Source="{Binding CoverImage}" Stretch="UniformToFill" Grid.Row="0"/>
                        
                        <!-- Fallback Icon if no cover -->
                        <Border Grid.Row="0" Background="#11FFFFFF" Visibility="{Binding NoCoverVisibility}">
                            <FontIcon Glyph="&#xE142;" FontSize="48" Foreground="{ThemeResource TextFillColorDisabledBrush}"/>
                        </Border>

                        <!-- Album Info -->
                        <StackPanel Grid.Row="1" Padding="12" Spacing="4">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="16" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding Description}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" MaxLines="2" TextTrimming="CharacterEllipsis" TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding CountText}" FontSize="12" Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <Border x:Name="EmptyState" Grid.Row="1" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Spacing="12" HorizontalAlignment="Center">
                <TextBlock Text="&#x1F5C2;" FontSize="48" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="No albums yet" Style="{StaticResource SubtitleTextBlockStyle}" HorizontalAlignment="Center"/>
                <Button Content="Create album" Style="{StaticResource AccentButtonStyle}" Click="OnCreateAlbum" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>