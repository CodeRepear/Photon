<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Photon.Views.GalleryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Loaded="OnPageLoaded"
    Unloaded="OnPageUnloaded"
    AllowDrop="True"
    DragOver="OnDragOver"
    Drop="OnDrop">

    <Page.Resources>
        <MenuFlyout x:Key="ItemContextFlyout">
            <MenuFlyoutItem Text="Open" Click="OnContextOpen"/>
            <MenuFlyoutItem Text="Open in folder" Click="OnContextReveal"/>
            <MenuFlyoutSeparator/>
            <MenuFlyoutItem Text="Copy path" Click="OnContextCopyPath"/>
        </MenuFlyout>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Grid Grid.Row="0" Margin="8,8,8,0" Height="48">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <AutoSuggestBox
                    x:Name="SearchBox"
                    PlaceholderText="Search photos..."
                    Width="260"
                    QueryIcon="Find"
                    TextChanged="OnSearchChanged"/>
                <ComboBox x:Name="SortCombo" Width="150" SelectionChanged="OnSortChanged">
                    <ComboBoxItem Content="Newest first" Tag="DateDesc" IsSelected="True"/>
                    <ComboBoxItem Content="Oldest first" Tag="DateAsc"/>
                    <ComboBoxItem Content="Name A-Z" Tag="NameAsc"/>
                    <ComboBoxItem Content="Largest size" Tag="SizeDesc"/>
                </ComboBox>
                <ComboBox x:Name="GroupCombo" Width="150" SelectionChanged="OnGroupChanged">
                    <ComboBoxItem Content="Group by Date" Tag="Date" IsSelected="True"/>
                    <ComboBoxItem Content="Group by Folder" Tag="Folder"/>
                    <ComboBoxItem Content="Group by Format" Tag="Format"/>
                    <ComboBoxItem Content="No grouping" Tag="None"/>
                </ComboBox>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
                <Button Background="Transparent" BorderThickness="0" Click="OnRescanClick">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE72C;" FontSize="15"/>
                        <TextBlock Text="Rescan" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Background="Transparent" BorderThickness="0" Click="OnAddFolderClick">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE710;" FontSize="15"/>
                        <TextBlock Text="Add folder" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Background="Transparent" BorderThickness="0" Click="OnBatchOpsClick">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE71D;" FontSize="15"/>
                        <TextBlock Text="Batch ops" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Gallery content: built in code-behind for justified layout -->
        <ScrollViewer
            Grid.Row="1"
            x:Name="GalleryScroll"
            Padding="16,8,16,16"
            HorizontalScrollMode="Disabled"
            VerticalScrollBarVisibility="Auto"
            SizeChanged="OnScrollSizeChanged">
            <StackPanel x:Name="GalleryRoot"/>
        </ScrollViewer>

        <!-- Empty state -->
        <Border
            x:Name="EmptyState"
            Grid.Row="1"
            Visibility="Collapsed"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <StackPanel Spacing="12" HorizontalAlignment="Center">
                <TextBlock Text="&#x1F5BC;" FontSize="48" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="No photos yet" Style="{StaticResource SubtitleTextBlockStyle}" HorizontalAlignment="Center"/>
                <TextBlock Text="Add a folder to get started." Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                <Button Content="Add folder" Style="{StaticResource AccentButtonStyle}" Click="OnAddFolderClick" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
